export default {
  totalAdmin(state,data){
    state.totalData.admin = data
  },
  totalUser(state,data){
    state.totalData.user = data
  },
  menuRoute(state,data){
    state.menuRoute = data
  }
}